import 'package:flutter/material.dart';

class EmptyCharacterWidget extends StatelessWidget {
  const EmptyCharacterWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
